with "config/uml2code_config.gpr";
with "plantuml_parser";
with "templates_parser";

project Uml2Code is
   for Source_Dirs use ("src", "config");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";
   for Main use ("uml2code.adb");

   package Compiler is
      for Default_Switches ("Ada") use
        ("-gnat2022", "-gnatX",
         "-gnata", "-gnatwa", "-gnatVa");
   end Compiler;

   package Install is
      for Artifacts (".") use ("resources");
   end Install;
end Uml2Code;
